package observer;

/** This is an interface that is implemented by the resident objects(Resident1, Resident2, Resident3) that all use the method update() to
 * be updated of a button pressed */

public interface Observer {
        void update ();
    }
